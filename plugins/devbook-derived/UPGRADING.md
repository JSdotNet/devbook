# Upgrading devbook-derived

Behaviour changes a consumer would notice, newest first.

## 1.1.0: the first release

Nothing precedes it. The tooling here shipped inside `devbook` until 1.1.0 and moved out in
the same release, per `.devbook/arc42/adr/77-the-tooling-is-devbook-deriveds.md`; no
consumer had installed either, so there is nothing to upgrade from.
